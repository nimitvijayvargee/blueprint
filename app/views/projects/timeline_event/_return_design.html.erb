<% if event[:feedback] %>
  <%= render "timeline_event_card" do %>
    <div class="flex space-x-4">
      <% if event[:user] %>
        <p class="grow"><%= render "shared/user_inline", user: event[:user] %> requested changes for <%= project.title %> <%= time_ago_in_words(event[:date]) %> ago</p>
      <% else %>
      <% end %>
    </div>
    <div class="timeline-journal">
      <p><%= event[:feedback] %></p>
      <% if event[:tier_override].present? %>
        <p><strong>Recommended tier:</strong> <%= event[:tier_override] %></p>
      <% end %>
      <% if event[:grant_override_cents].present? %>
        <p><strong>Grant to expect:</strong> $<%= sprintf("%.2f", event[:grant_override_cents] / 100.0) %></p>
      <% end %>
    </div>
  <% end %>
<% else %>
  <%= render "timeline_event_basic", icon: "icons/return.svg" do %>
    <% if event[:user] %>
        <p><%= render "shared/user_inline", user: event[:user] %> requested changes for <%= project.title %> <span title="<%= event[:date] %>"><%= time_ago_in_words(event[:date]) %> ago</span></p>
    <% else %>
      <p><%= project.title %> was returned needing changes <span title="<%= event[:date] %>"><%= time_ago_in_words(event[:date]) %> ago</span></p>
    <% end %>
  <% end %>
<% end %>
